Analyze this Flutter project and perform a complete iOS configuration audit by examining the project's `pubspec.yaml`, `ios/Podfile`, `ios/Runner/Info.plist`, `AppDelegate.swift` (or Objective-C equivalent), entitlements files, and all installed Flutter packages. Read the official documentation for every package used in the project and identify all required iOS-specific setup steps, including but not limited to Info.plist permission keys, Podfile changes, deployment target requirements, App Transport Security settings, URL schemes, background modes, capabilities, entitlements, push notification setup, microphone permissions, speech recognition permissions, camera permissions, photo library permissions, Bluetooth permissions, local network permissions, location permissions, file access permissions, and any native iOS initialization code. Verify whether each required configuration is already present, identify any missing or incorrect setup, explain why it is needed, specify the exact file where changes must be made, provide the exact code or XML snippets that should be added or modified, and generate a detailed package-by-package report showing the package name, required iOS configuration, current status (Configured or Missing), and the complete set of changes needed. Do not assume any configuration has been completed; instead, thoroughly inspect the project and produce all necessary updates required for the application to build, run, and function correctly on iOS.
